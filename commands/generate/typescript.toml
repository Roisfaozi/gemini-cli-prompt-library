prompt = """
# Generate Code in TypeScript

Please generate a TypeScript program that solves the following problem:

{{args}}

## Requirements:
- The code should be modern, idiomatic TypeScript.
- Include comments to explain the logic.
- Provide a clear example of how to use the code.
- Ensure the code is runnable and compiles correctly.
- Use static types effectively.

## Example:

User request: "a class for a simple calculator"

```typescript
class Calculator {
  add(a: number, b: number): number {
    return a + b;
  }

  subtract(a: number, b: number): number {
    return a - b;
  }
}

// Example usage:
const calculator = new Calculator();
console.log(calculator.add(5, 3));
```
"""
